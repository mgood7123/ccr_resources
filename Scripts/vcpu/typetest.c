#include "../Shell/builtins/printfmacro.h"
#include "../Shell/builtins/regex_str.h"
void main() {
	str_new(type)
	pi(isdigit(('d')))
	pi(isdigit('d'))
	pi(isdigit('0'))
	str_insert_char(type, 'd');
	str_info_(type);
	str_free(type)
}
